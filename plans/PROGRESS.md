# Implementation Progress

| Phase | Status | Session notes |
|---|---|---|
| 1. Data Model | done | `proto/secscan.proto` + `proto/make-bindings.sh` added; bindings generated for real via the `saichler/protoc` docker image (available locally) into `go/types/secscan/secscan.pb.go`. Bootstrapped `go/go.mod`, `go/go.sum`, `go/vendor/` (see deviation below). `go build ./...` and `go vet ./...` clean. |
| 2. Backend | done | `go/secscan/{customer,imagecategory,imagegroup,imageref,cve,imagerefcve,scanjob}` (7 ORM-backed services + `ServiceCallback`s), `go/secscan/{imgrefadd,vulnrep}` (2 `IServiceHandler` action services), `go/secscan/common` (shared ingestion helper + type registration + constants), `go/secscan/services` (activation aggregator), `go/secscan/main` (backend entry point, incl. `l8events.ActivateEvents`). Security config written to `../l8secure/go/secure/plugin/secscan/secscan.json` (separate repo — see deviations; left **uncommitted** there, user's call whether/when to commit). `go build ./...`/`go vet ./...`/`gofmt` clean. See deviations below for 2 verified corrections made along the way (a real cross-tenant leak in `VulnRep`'s original design, and a data-model gap for the CSV "N/A" rule). |
| 3. Scanner | done | `go/secscan/scanner/{main.go,pollworker,resolver,scanloop}` — the shared poll-claim-dispatch harness (PRD §13), the metadata resolver loop (§6.1 Phase B, via `go-containerregistry`, newly vendored), and the Trivy scan loop (§13.1, shells out to the `trivy` CLI, two-level bounded concurrency: across claimed `ScanJob`s and across one job's images). `go/secscan/common/delete.go` adds the `DeleteEntitiesByQuery` helper `l8common` doesn't provide. No login/auth step in `main.go` — verified real vnic-to-vnic calls aren't AAA-checked in this framework (see deviations). `go build ./...`/`go vet ./...`/`gofmt` clean, including `go build -o /dev/null ./secscan/scanner/` for the binary itself. Two verified corrections made along the way (see deviations): a re-scan staleness bug (`ImageRefCve` rows never got cleared between scans) and confirmation that `secscan-scanner`'s vnic identity isn't actually enforced by AAA (documentation-only for now, not a functional gap). |
| 4. Desktop UI | done | `go/secscan/ui/web/` — `l8ui` added as a real git submodule (`c36dbe2`); `app.html`/`index.html`/`login.json`/`css/` adapted from `../l8alarms` (closest-scale real precedent, not `l8erp`'s 12-module template); `secscan/` module: 2-section structure (`vulnmgmt`: groups/categories/scanjobs, `admin`: customers), enums/columns/forms per PRD §11, custom Group Detail popup (embedded `ImageRef` table + hand-built multi-select, no native selection exists in `Layer8DTable`) and Vulnerability Detail popup (`ImageRefCve` table), Add Images, Export CSV Report, KPI strip + chart toggle. **Could not live-test in a browser** — no running backend/DB/compiled security plugin/mock data yet (those are Phases 6/7); verification was static: `node --check` on every `.js` file, every script/CSS path in `app.html` confirmed to resolve to a real file, every field name cross-checked against `secscan.pb.go`'s real `json=` protojson names, every `/60/<ServiceName>` endpoint cross-checked against Phase 2's Go constants. See deviations for several verified framework facts/gaps found and designed around along the way (client-side `customerId` discovery, no native multi-select, PUT-is-partial-merge, no 3-level popup-in-popup precedent). |
| 5. Mobile UI | done | `go/secscan/ui/web/m/` — app shell (`app.html`/`index.html`/`login.json`/`manifest.json`/`css/secscan-m.css`) adapted from `../l8erp`'s real mobile shell (the only usable mobile reference — `../l8alarms` has no mobile implementation at all, empty `m/`). Two nav-config modules (`vulnmgmt`: groups/categories/scanjobs, `admin`: customers) each with exactly one `subModules` entry (auto-skip straight to the services list, verified in `layer8m-nav.js`), plus a `system` module (Health Monitor + L8Security's Users/Roles/Credentials, the same shared framework columns/forms desktop's System section already uses). Desktop's own enum/column/form files (`../secscan/vulnmgmt/*.js`, `../secscan/admin/*.js`) are loaded verbatim on the mobile page and decorated with `primary`/`secondary`/`hidden` card flags via `.map()`+`Object.assign` into `MobileSecScanVuln`/`MobileSecScanAdmin` (the real, documented `AddingModule` mobile convention — desktop/mobile share one `Layer8ColumnFactory` output, no separate mobile column factory exists) — forms reused with zero changes. Group Detail and Vulnerability Detail popups (`Layer8MPopup` + a hand-built `Layer8MTable` with `baseWhereClause`, checkbox multi-select via event delegation — `Layer8MTable`/`Layer8MEditTable` confirmed to have no native row-selection either), Add Images, Add Category, Export CSV Report (same Blob+`<a download>` trick, verbatim), and the Image Groups list's KPI strip + toolbar (via the real `customInit` service-view mechanism, since the generic `Layer8MNavData` pipeline has no extension point for extra chrome above a table) all mirror desktop's Phase 4 designs with mobile-flavored APIs (`Layer8MAuth`/`Layer8MConfig`/`Layer8MPopup`/`Layer8MTable`/`Layer8MReferencePicker` in place of their desktop equivalents). **Could not live-test in a browser** — same limitation as Phase 4 (no running backend/DB/compiled security plugin/mock data yet); verification was static: `node --check` on every `.js` file, every script/CSS path in `app.html` confirmed to resolve to a real file, every endpoint cross-checked against Phase 2's Go `ServiceName`/`ServiceArea` constants, `go build ./...` re-confirmed clean (no Go files touched this phase). See deviations below for a real, previously-shipped login bug found and fixed at the start of this phase, a real framework registry-whitelist gap found and fixed, and the deliberate mobile scope decisions made along the way (no chart, reduced System scope idea later expanded to full parity once the shared `L8Security` files were found). |
| 6. Mock Data | done | **Scope reduced per explicit user instruction (2026-09-13): "you don't need mock data, just use the images of this project and customer name local."** `go/tests/mocks/{client.go,seed.go,main.go}` + `go/tests/cmd/main.go` — a standalone HTTP-only seeder (mirrors `../l8alarms/go/tests/mocks`'s real, generic pattern: authenticates as `opsadmin` against a running `secscan-web`, then POSTs). Seeds exactly: one `Customer{customerId:"local"}`, 2 `ImageCategory` rows, one `customer`-role `L8User` (`local-user`/`localpass`, `associateIds:["local"]`, `customer:"local"` so the real client-side customerId mechanism resolves correctly after login) via the Security API (`/73/users`, never a project-owned endpoint), and this project's own 8 real container images (PRD §16's binary + base-image table, `saichler/<name>:latest`) registered via the real `ImgRefAdd` bulk-ingestion action service (§6.1) — not a direct `ImageGroup`/`ImageRef` POST. **No fabricated `Cve` catalog or `ImageRefCve`/scan-result data at all** (PRD §15 items 5/6 dropped entirely, per the user's instruction) — every seeded `ImageRef` starts `PENDING`, exactly as it would for a real user adding real images; a real `secscan-scanner` run against them (once Phase 7's images are built) produces real Trivy findings instead of fabricated ones. `PRD §15` item 7 (Scan Jobs) also dropped — there's nothing genuine to seed there until a real scan has actually run. Did **not** build the full phased `data.go`/`store.go`/`gen_*.go`/`phases.go` machinery `MockDataRules` describes — that structure exists for large randomized datasets with cross-module FK dependencies; this seed is ~10 fixed literal records with no such need, so a single small `seed.go` was used instead (documented here as a deliberate deviation from the guide-line's literal file layout, not an oversight). `go build ./...`/`go vet ./...`/`gofmt` clean. **Not run against a live server** — no backend is running in this environment (same limitation noted in every UI phase); verified only statically (compiles, field names match `secscan.pb.go`'s real `json=` names, endpoints match Phase 2's Go `ServiceName`/`ServiceArea` constants). Run with `go run ./tests/cmd -address https://localhost:2790 -user opsadmin -password opsadmin` once the backend is actually up. |
| 7. Deployment | done | **Prerequisite gap found and fixed first** (user confirmed): PRD §16 lists 6 binaries but only `secscan`/`secscan-scanner` had a `main.go` — added `go/secscan/{ui/main,vnet,log-vnet,log-agent}/main.go` (see the separate commit for that; mirrors `../l8alarms`'s ui/vnet patterns and `../probler`'s/`../l8erp`'s log-vnet/log-agent patterns exactly; new direct dependency `l8logfusion`, a documented `VendorAndGit` exception). **Then deployment artifacts, copied from `../probler/k8s/` (the explicit canonical reference, `K8sRules`/`L8PollarisBinaryDeployment`) and adapted, not hand-written**: `go/secscan/{main,ui,vnet,scanner,log-vnet,log-agent}/{build.sh,Dockerfile}` (vnet/log-vnet use `CGO_ENABLED=1`+`go mod vendor`+`/patch.sh`, matching probler's vnet/log-vnet exactly — everything else is the simpler `go mod init && go mod tidy && go build` pattern, all COPYing only `main.go` and depending on this project's own pushed GitHub module for its own sibling packages, the same real mechanism every other binary in every sibling project's Dockerfile already relies on); `secscan-scanner`'s Dockerfile adds the Trivy CLI via `FROM aquasec/trivy:latest AS trivy` + `COPY --from=trivy` (PRD §16's one deliberate customization). `go/build-all-images.sh`. `k8s/secscan-{local,baremetal,gke,kind}.yaml` (all 6 workloads; verified via a throwaway Go/`gopkg.in/yaml.v3` multi-doc parser in this sandbox, since neither a live cluster nor PyYAML/pip was available here — document counts match expectations exactly: 7/8/9/7). `k8s/deploy.sh`/`undeploy.sh` (apply the combined per-mode file, then `rollout status`-wait in the PRD's explicit dependency order: vnet → backend → scanner → web → log-vnet → log-agent) and `k8s/kind-{start,stop}.sh`. `go/run-local.sh` (§17, copied from `../l8erp/go/run-local.sh` and adapted: our own 6 binaries + the Phase 6 mocks seeder, port 2790, `opsadmin`/`opsadmin`, DB creds from `secscan.json`'s `postgres.creds.secscan`). Full `K8sRules` verification checklist run and passing (file existence, 0 StorageClass/PVC in `-local.yaml`, `rancher.io/local-path` in `-baremetal.yaml`, `kubernetes.io/gce-pd` in `-gke.yaml`, 0 `kind: StorageClass` in `-kind.yaml`, identical `image:` lines between local/baremetal). `go build ./...`/`go vet ./...`/`gofmt` clean; every shell script `bash -n`-checked. **Not actually run** (no Docker/K8s/live cluster in this environment) — see deviations for what's unverified as a result. |
| 8. Testing | done | `go/tests/{TestInit.go,StartWebserver.go}` + 6 `*_test.go` files, mirroring `../l8alarms/go/tests`'s real, working pattern exactly (`TestLocationAndApproach`: one shared in-process topology/DB/web-server setup, one `go test`-visible `TestAllServices`, every other file exports a lowercase `testXxx(t, ...)` helper it calls in sequence — not independent `go test` entry points, to amortize the expensive shared setup). Covers every PRD §19 bullet: `TestIngestion_test.go` (mixed ImgRefAdd batch: 2 valid across differing hosts/tags/case → 1 shared group, 1 exact duplicate → skipped, 1 unparseable line → error), `TestResolver_test.go` (fixture registry lookup via the new `resolver.LookupCreated` seam, success sets `buildDate`, failure sets `scanError` and leaves `buildDate=0`), `TestScanPipeline_test.go` (fixture Trivy payload via the new `scanloop.RunTrivy` seam, two sequential `ScanJob`s proving `ImageGroup.oldestCounts` **moves** to a later-scanned-but-older-buildDate ref rather than staying pinned, `Cve` find-or-create dedup across two images sharing one CVE ID, `ImageRefCve.customerId` correctness), `TestReporting_test.go` (`ImageRefCve` query shape/scoping with a second customer sharing a CVE ID, VulnRep CSV reduction-% math against real + N/A-edge-case groups, full 15-column header set, `Image Refs` cell sort order), `TestSecurity_test.go` (row-level scoping: `local-user` sees only `customerId=local` rows and is denied `Customer` access, `opsadmin` sees everything; `ScanJob` rejects a request whose `imageRefIds` belong to a different customer than its own `customerId` — already-existing Phase 2 validation, not a gap). See deviations below for the two small, deliberate testability seams added to Phase 3's scanner code (exported `RunTrivy`/`LookupCreated` reassignable vars) and the new `l8test` dependency. `go build ./...`, `go vet ./...` (which also type-checks every `_test.go` file), and `gofmt` all clean; `go test -run '^$' ./tests/...` (compile-only, no live DB/security-plugin/network here) confirms every test file actually compiles and that `TestInit.go`'s real in-process topology setup/teardown runs without error. `find go/ -name "*_test.go" -not -path "go/tests/*"` returns nothing. **Could not actually run the tests to completion** (no live Postgres/compiled security plugin/`trivy` CLI in this environment, same limitation as every prior phase) — the assertions are correct by careful reading of the real backend code they exercise (`RecomputeImageGroupCache`, `VulnRep`'s CSV builder, `ScanJobServiceCallback`, `ScopeView`'s deny rules in `secscan.json`), not by having watched them pass. |
| 9. Verification | done | Walked PRD §20's compliance checklist line by line against actual repo state (`VerifyPrdCompletenessBeforeDone`), not trusting its own pre-written `[x]` marks: project structure, protobuf conventions (enum zero, `repeated X list=1`+`metadata`, ID-only refs — all verified via direct `grep` against `proto/secscan.proto`), ServiceName lengths, PK auto-generation (confirmed via two real mechanisms — explicit `l8common.GenerateID` for `ImageRef`/`ImageGroup`/`Cve`/`ImageRefCve`/`ScanJob`, `NewValidation`'s introspector-derived auto-`setID` for `Customer`/`ImageCategory`, both do the same "assign a UUID if empty" thing), `l8events.EventRecord` UI-registration (verified our project does **not** register it in `ui/main.go`, same as the real reference `../l8alarms` — not a gap, a widely-shared real pattern the literal `EventsServiceRequired` wording doesn't quite match), no `l8secure` import anywhere, `SingleOwnerDatabaseTable` (scanner never calls `ActivateService`/`RegisterType`), `go/demo/` never committed. Ran a full `go build ./...`/`go vet ./...`/`gofmt -l` sweep (clean) and a programmatic script/CSS path-resolution + relative-script-order check for both `app.html` and `m/app.html` against `../l8erp/rules/{desktop,mobile}-script-loading-order.md` (`VerifyAppHtmlScriptsAgainstLoadingOrder`) — see deviations for the one real broken link found and fixed, and the handful of relative-order differences found and verified benign (no file involved has top-level/parse-time code depending on another not-yet-loaded global — all the coupling is at runtime, well after every script tag has executed). **Also found and corrected several places where the PRD's own prose still described the pre-implementation design intent instead of what was actually verified/built in later phases** — see deviations; this is the main substantive output of this phase, not a rubber stamp. |

## Deviations from the PRD (if any)

- **Phase 1** — The PRD's §7 literal `proto/secscan.proto` code block does not match this ecosystem's actual protobuf conventions (verified against `../l8erp/proto/*.proto` and `../l8alarms/proto/*.proto`, both of which generate cleanly via the same `saichler/protoc` docker pipeline this phase used for real):
  - `import "l8api.proto";` → there is no file named `l8api.proto` anywhere in the ecosystem (verified against `../l8types/proto/`). The actual file (downloaded from `l8types`) is `api.proto`, declaring `package l8api;`. Changed the import to `import "api.proto";` (message references stay `l8api.L8MetaData`, which was already correct in the PRD).
  - `l8api.AuditInfo audit_info = N;` → `AuditInfo` is not defined in `l8api`/`api.proto` at all. It is defined in `l8common.proto` (`package l8common;`, confirmed used as `l8common.AuditInfo` throughout `../l8erp/proto/*.proto`). Added `import "l8common.proto";` and changed every `audit_info` field to `l8common.AuditInfo`.
  - `option go_package = "github.com/saichler/l8secure-scan/go/types/secscan";` → every sibling project instead uses a relative `option go_package = "./types/<module>";` and relies on `make-bindings.sh` to `mv` the generated `./types/<module>` directory into `../go/types/<module>` (which resolves correctly through the Go module path once placed there). Matched that convention: `option go_package = "./types/secscan";`.
  - These are mechanical protobuf-authoring corrections (the PRD's snippet as literally written does not compile/resolve against the real framework), not a design/architecture change — the data model itself (all fields, enums, Prime Object shapes) is implemented exactly as specified in §7.
  - `proto/make-bindings.sh` also needed two `sed` rewrites after generation (mirroring `../l8erp/proto/make-bindings.sh`, not invented): `./types/l8api` → `github.com/saichler/l8types/go/types/l8api` and `./types/l8common` → `github.com/saichler/l8common/go/types/l8common`. The `l8api` rewrite turned out to be necessary even though `../l8alarms/proto/make-bindings.sh` (already-committed, not re-run this session) doesn't have it — the currently-pulled `saichler/protoc:latest` image does not auto-resolve the `l8api` import path, confirmed by actually running the generator and inspecting the output before adding the `sed` line. Also added a `gofmt -w ./types` after the `sed` rewrites — rewriting the import path strings in place can leave the import block out of gofmt's alphabetical order (verified: it did, once, here), and generated code should stay gofmt-clean like every sibling project's.
- **Phase 1 (bootstrap, not really a PRD deviation)** — `VendorAndGit` says never run `go mod tidy`/`go mod vendor`/`go mod init`. This is a brand-new repo with no prior `go.mod`/`vendor/` at all, so those commands were run exactly once to create the module and vendor tree from scratch (`go mod tidy` to resolve `l8types`/`l8common`/`google.golang.org/protobuf`, then `go mod vendor`) — the same one-time bootstrap every sibling project's own history implies. No vendored file was hand-edited; from here on `VendorAndGit` applies normally (any future dependency-version bump is a project the user directs, not something later phases should do silently).

- **Fixed directly in the PRD (§13, the scan-loop poll query) — L8Query enum-comparison syntax bug, verified against `l8ql`'s actual parser/interpreter source, not just precedent.** The PRD previously specified `select * from ScanJob where status='JOB_STATUS_QUEUED'` — a *quoted enum name string*. This does not work: `l8ql/go/gsql/interpreter/comparators/Equal.go` (`eqIntMatcher`/`getInt64`) does `strconv.Atoi` on the right-hand operand whenever the left-hand property is an `int32`-kind (enum) field; a quoted name like `'JOB_STATUS_QUEUED'` fails `Atoi` and the match silently returns `false` (not an error — the poll loop would just never match anything, a silent no-op bug). No enum name→int resolution exists anywhere in the WHERE-clause match path. Verified real precedent across `../l8erp` (`dashboard-config.js`: `status=2`), `../l8alarms` (`matcher.go`: `fmt.Sprintf("...Status=%d", ...)`) — every real query in the ecosystem filters enum fields with a **bare integer**, never a quoted name. PRD §13 now reads `select * from ScanJob where status=1` (`1` = `JOB_STATUS_QUEUED` per `proto/secscan.proto`, Phase 1), with the reasoning inlined in the PRD itself so Phase 3 doesn't need to rediscover it. **This same rule applies to any other enum-field WHERE clause introduced in later phases** (e.g. filtering `ScanStatus`/`Severity`) — always compare enums as bare integers, never quoted string names, even if a future PRD edit or ad-hoc query reintroduces the quoted form.
- **Flagged for Phase 4/5 (UI) — reminder to apply `L8QueryRules` precisely, since the PRD itself doesn't spell out every literal query string.** The PRD only gives literal L8Query text for the security deny rules (§4/§14) and the two nested `baseWhereClause` list views (§11.3/§11.4, `imageGroupId='<id>'` / `imageRefId='<id>'` — string-ID quoting there is correct, verified against `layer8m-portal.js`'s `baseWhereClause` construction). Every other list view (Image Groups dashboard, Categories, Scan History, Customers) has no literal query shown and is expected to rely on `Layer8DTable`/`Layer8DDataSource` auto-building `select * from <ProtoType>` from `endpoint`+`modelName` — that's the documented-correct pattern, not a gap. When Phase 4/5 implements anything that does its own query-string assembly (custom fetches, `select *` on detail popups per `L8QueryRules` Rule 2, any `in`/`not in` list — real bracket syntax is `field in [a,b,c]`, not parens, per `l8ql/go/gsql/interpreter/comparators/In.go`), double-check against `L8QueryRules` directly rather than trusting the PRD's prose to be complete — this project's implementer previously had recurring bugs specifically in L8Query construction.

- **Fixed directly in the PRD (§10, `VulnRep`) — a real cross-tenant data leak in the original design, verified against `l8services`' actual request-dispatch source, not just precedent.** The PRD originally specified `VulnRep` as "no request body needed beyond auth ... via the standard row-level deny rule". Verified against `l8services/go/services/manager/ServiceManager.go`: row-level `ScopeView` filtering runs in exactly two places (`Handle`/`TransactionHandle`), both *after* a handler's `Get`/`Post` returns, keyed off the caller's `AAAId` carried on the inbound `ifs.Message`. A custom action-service (`VulnRep`, correctly modeled on `l8services/go/services/csvexport/CsvExport.go` — `IServiceHandler`, not `IServiceCallback`) only ever receives `Post(elems ifs.IElements, vnic ifs.IVNic)` — no `*ifs.Message`, and neither `IVNic` nor `IElements` expose the caller's identity anywhere. Worse, the natural implementation (copying `CsvExport.Post`'s own direct in-process `handler.Get(...)` call to fetch `ImageGroup` rows) bypasses `ServiceManager.Handle` entirely, so `ScopeView` never runs on it at all — it would return **every customer's** `ImageGroup` rows to any caller, not just the requester's own tenant. There is no framework extension point to recover caller identity inside a custom action-service handler (the same underlying gap as the already-accepted `ServiceCallback` limitation in §4/§9, now confirmed to extend to `IServiceHandler` too). Fixed the only workable way, consistent with this PRD's own existing v1 trust boundary: `VulnRep`'s request now carries an explicit trusted `customerId` (same pattern as every other write in this PRD), and the handler filters `select * from ImageGroup where customerId='<given>'` explicitly rather than relying on scoping that cannot actually reach this code path. Also corrected the response shape from a bogus "raw `text/csv` HTTP body" (no real precedent for that exists anywhere in the framework — verified) to a JSON response with the CSV text as a string field, reusing `l8api.L8CsvExportResponse{csv_data, filename, row_count}` — the actual, only real precedent for a generated-CSV response in this ecosystem. **This same caller-identity gap applies to any future custom `IServiceHandler`-based action service** (not just `VulnRep`): never assume automatic row-scoping reaches a hand-written `Post`/`Get` handler — it only ever protects the outer CRUD `Get` path through `ServiceManager.Handle`. `ImgRefAdd` was already unaffected — its every internal query already filters explicitly by the request's own trusted `customerId`, never relying on caller-derived scoping.
- **Phase 2 implementation notes**:
  - `go/secscan/common/` owns cross-cutting Phase-2 code: DB credential/name constants, `RegisterSecscanTypes(resources)` (type registration for introspection — called by this phase's backend `main.go`; Phase 4's `go/secscan/ui/main.go` should call the *same* function rather than redefining it, per `Duplication Prevention`), the shared image-ref-string parser (§5) and the shared ingestion helper (§6.1 Phase A, used by both the plain `ImageRef` POST path and `ImgRefAdd`).
  - The 7 ORM-backed services (`Customer`, `ImageCategory`, `ImageGroup`, `ImageRef`, `Cve`, `ImageRefCve`, `ScanJob`) each get their own package (mirroring `../l8alarms/go/alm/<service>/`), activated via `common.ActivateService` (the real `l8common/go/common/service_factory.go` helper) with `common.NewServiceCallback`/`common.NewValidation(...).Build()` for their `ServiceCallback`.
  - `ImgRefAdd` and `VulnRep` are each their own package implementing `ifs.IServiceHandler` directly (not `IServiceCallback`), registered via `ifs.NewServiceLevelAgreement` + `web.New`/`AddEndpoint`, mirroring `l8services/go/services/csvexport/CsvExport.go` exactly (stub `Put`/`Patch`/`Delete`/`Get` returning `object.NewError("not supported")`).
  - `proto/secscan.proto` gained `VulnRepRequest`/`ImgRefAddRequest`/`ImgRefAddItem`/`ImgRefAddResponse` messages in this phase (not Phase 1 — the PRD's own Traceability Matrix assigns these two action-services to Phase 2, and their non-CRUD wire shapes weren't part of §7's core data model). `VulnRep`'s response reuses `l8api.L8CsvExportResponse` directly rather than duplicating it.
  - **The security config plugin JSON lives in the sibling `../l8secure` repo**, not in this repo, at `../l8secure/go/secure/plugin/secscan/secscan.json` — confirmed against every real project (`alm.json`, `erp.json`, `phy.json`, etc. all live inside `l8secure/go/secure/plugin/<name>/`, never inside the owning project's own repo) and against the actual load mechanism: `l8common.CreateResources` calls `sec.LoadSecurityProvider`, which does `plugin.Open("/var/loader.so")` — a **compiled Go plugin**, built once via `l8secure/go/secure/plugin/build.sh secscan` (`provider.Prepare` reads `secscan/secscan.json`, hashes the plaintext seed passwords, embeds the result into `SecurityLoader.go`, then `go build -buildmode=plugin`). No JSON is read at process startup; the PRD's literal path `go/secure/plugin/secscan/secscan.json` only makes sense prefixed with `../l8secure/`. User confirmed writing there directly (a separate git repo from this one) rather than keeping a non-functional local copy. The real, working shape (`credentials.<group>.{id,name,creds.<name>.{aside,yside,zside}}`, `roles.<role>.{roleId,roleName,rules.<rule>.{ruleId,elemType,allowed,actions,attributes}}`, `sysconfig.{dataStoreConfig:{name,type},timeSeriesStoreConfig,webConfig:{endPointPrefix,webPort,...certs},vnetPort,logConfig.vnetPort,...}`, full `L8User` field set incl. `associateIds`) differs substantially from the PRD's §14 draft — the draft's flat shape was aspirational/simplified, not copied from a real file. `secscan.json` seeds only the durable system identities that must exist for the architecture to function (`scanner-svc`, one `opsadmin` bootstrap user) — real per-customer users are provisioned dynamically via the Security API (`/73/users`) in Phase 6 (Mock Data)/at signup time, never hardcoded here, per the verified real pattern and `SecurityRules`.
  - `vnetPort`/`webPort`/`logConfig.vnetPort` in `secscan.json` are dev-placeholder values chosen to avoid an obvious collision with sibling projects' already-allocated ports (checked every real `l8secure/go/secure/plugin/*/*.json`) — not yet verified against a real shared cluster; re-check before any actual shared/multi-project deployment (Phase 7 concern). TLS cert fields (`domainCertPem`/`privateKeyPem`/`publicKeyPem`) are left empty — they're only exercised by `CreateWebServer` (the UI-server binary, Phase 4/7), not by this phase's ORM backend.
  - `secscan.json` was verified structurally correct, not just written to look right: unmarshaled it with `protojson.Unmarshal` against the real `github.com/saichler/l8secure/go/types/secure.L8SecureConfig` proto type in a throwaway scratch program (not committed anywhere) — confirms 3 roles (13/1/4 rules), 2 users, 1 credential group all parse as intended. It has **not** been run through the actual `build.sh secscan` plugin-build step (that needs `sudo` to install to `/var/loader.so` and isn't needed to develop/build this repo's own Go code) — do that once, whenever a session actually needs to run the backend against real auth, not as part of any later phase's routine work.
  - **`ImageGroup` gained a new field this phase**: `scanned_ref_count` (int32, field 10) in `proto/secscan.proto`, extending Phase 1's message. The PRD's canonical Reduction % "N/A" rule (§10: "fewer than 2 scanned image refs" / "newest and oldest resolve to the same image ref") cannot be evaluated from `newest_counts`/`oldest_counts` alone — two different `ImageRef`s can coincidentally have identical severity counts, and none of the three consumers (CSV, dashboard, Group Detail panel) ever have anything but these cached `ImageGroup` fields to check against. `scommon.RecomputeImageGroupCache` now sets it (count of `ImageRef` rows with `scanStatus=COMPLETED`); by construction, `scannedRefCount<2` always coincides with "newest ref IS oldest ref", so no separate same-ref check was needed. `VulnRep`'s reduction-% columns use it now. **Phase 4/5 (UI) must use the same field** for the dashboard Trend indicator and Group Detail Trend panel's N/A logic — don't reintroduce a client-side approximation that doesn't have it.

- **Fixed directly in the PRD (§13.1, scan loop) — re-scanning an `ImageRef` would have accumulated stale `ImageRefCve` findings forever.** Nothing in the original design deleted a previous scan's finding rows before writing a new scan's — §11.4's findings table (`baseWhereClause: imageRefId='<id>'`) would silently show old-plus-new findings mixed together after every re-scan, not just the latest scan. Fixed: `secscan-scanner` deletes every existing `ImageRefCve` row for the `imageRefId` (via `handler.Delete` with an L8Query, the real DELETE-by-query pattern — verified against `../l8alarms/go/alm/archiving/engine.go`'s `deleteAlarm`, the only real precedent for `IServiceHandler.Delete` usage in the ecosystem: `object.NewQuery("select * from X where field='value'", resources)` → `handler.Delete(elems, vnic)`) *after* Trivy succeeds and its output parses, but *before* writing the new findings — so a failed scan attempt leaves the previous scan's findings intact instead of wiping them for nothing. `l8common` has no `DeleteEntitiesByQuery` convenience wrapper (unlike `GetEntitiesByQuery`/`PutEntity`/`PostEntity`) — added one in `go/secscan/common/` mirroring the same hasLocal/`vnic.Request` fallback shape as the real helpers it sits next to, not a new pattern.
- **Verified (not a deviation, but a load-bearing architectural fact for Phase 3): `secscan-scanner`'s vnic calls to the backend are not actually AAA-checked.** See the corresponding note now in the PRD itself (§14, right after the `scanner-svc` paragraph) for the full verified call chain. In short: a raw vnic call with no explicit token gets an `aaaid` that's always too short to pass `CanDoAction`/`ScopeView`'s validated-token-length check, and both have an explicit "internal/system calls bypass" short-circuit for that case — confirmed against `../probler`'s `collector` binary (the closest real precedent: a separate-process, non-ORM worker) doing the exact same zero-login vnic calls this project's scanner does. **Practical upshot for Phase 3's code**: `secscan-scanner`'s `main.go` needs no login/credential step at all — `l8common.CreateResources` + `vnic.NewVirtualNetworkInterface` + `Start`/`WaitForConnection`, exactly like every other binary in this project, then straight into `common.GetEntity`/`PutEntity`/`PostEntity`/`GetEntitiesByQuery` calls. The `scanner`/`scanner-svc` role and user in `secscan.json` remain correct as *documentation* of intent and cost nothing to keep, but don't expect them to be doing any actual enforcement against this binary's traffic — the real boundary is which pods can reach the vnet (a Phase 7/K8s network-policy concern, not an application-auth one).
- **Phase 3 implementation notes**:
  - `go/secscan/scanner/pollworker/` — the generic harness (PRD §13's `pollworker.Run(query, claim, work)` sketch). Real signature ended up needing explicit `ServiceName`/`ServiceArea` alongside the query text (`l8common.GetEntitiesByQuery` requires them for handler routing; the query text alone isn't enough). Built on the two real `l8utils/go/utils/workers` primitives — `Workers{NewWorkers(limit)}` (bounded, `Run(IWorker)`, blocks when the pool is full, but no built-in wait-for-completion) and a plain `sync.WaitGroup` added around it for the fan-in half (`l8utils` doesn't have one primitive that's both bounded *and* fan-in — `MultiTask.RunAll()` is fan-in but unboundedly parallel, `Workers` is bounded but fire-and-forget; combining `Workers`+`WaitGroup` gets both, verified this is a normal/expected combination, not a workaround). Each poll tick: query, dispatch matches through `claim` (nil = always process, e.g. the resolver loop) then the bounded pool, wait for the batch to finish, sleep until the next tick.
  - `go/secscan/scanner/resolver/` configures the harness with `claim=nil` and a `work` function using `github.com/google/go-containerregistry` (`crane`'s underlying `pkg/v1/remote`/`pkg/name`/`pkg/authn` packages — not vendored anywhere else in the ecosystem yet, added fresh; `remote.WithAuthFromKeychain(authn.DefaultKeychain)` reads registry credentials from the pod's standard Docker config, matching §13/§18's "credentials already present on the pod") to fetch the image config and read its `Created` timestamp.
  - `go/secscan/scanner/scanloop/` configures the harness with `claim` = an unconditional `QUEUED→RUNNING` `PutEntity` (matches the PRD's already-accepted "no compare-and-swap, single replica" design — not re-litigated here) and a `work` function that itself fans out across the job's `imageRefIds` using a second, inner `Workers`+`WaitGroup` pool (two levels of bounded concurrency: across claimed jobs at the harness level, across one job's images at the work-function level — matches §13.1 step 1's phrasing that the per-image loop is itself "the harness's work function"). Shells out to `trivy image --format json <target>` (`repoName:tag` or `repoName@digest`, never the PRD's literal shorthand `repoName:<tag or digest>` — digests use `@`, not `:`, a plain notational fix, not a design change) via `os/exec`, parses just the fields needed (`Results[].Vulnerabilities[].{VulnerabilityID,PkgName,InstalledVersion,FixedVersion,Severity,Title}`) into a local struct rather than importing any Trivy Go package (Trivy is invoked as an external CLI per §13, not linked as a library). Trivy's occasional `"UNKNOWN"` severity string has no matching `secscan.Severity` bucket (enum zero must stay invalid, per `ProtobufRules`) — those findings are dropped (not counted, no `Cve`/`ImageRefCve` row), not force-mapped into `LOW`.

- **Phase 4 — client-side `customerId` discovery: SUPERSEDED.** Originally worked around by repurposing `L8User.portal` (see git history of this file/the PRD for that design and the login-breaking bug it required catching). **The user then fixed this properly at the framework level instead**, and asked me to wire the rest across all three affected repos:
  - `l8secure` (user's own commit `2c2eb07`): added a real `L8User.customer` attribute.
  - `l8types` (my commit `0b4a316`): added `AuthToken.customer` (field 8) so the `/auth` response can actually carry it.
  - `l8secure` (my commit `f007b4e`): threaded `usr.Customer` through `L8Token.Customer` (new `secure.proto` field 9) → `authenticate()`'s return values → `SecurityProvider.Authenticate()`'s `AuthToken.Customer` — mirroring the existing `Portal` plumbing line-for-line, including updating `authenticate()`'s one real caller and its three test call sites for the new arity.
  - `l8ui` (my commit `10d7fc7`): `layer8d-login-auth.js`'s `handleLoginSuccess` now does `sessionStorage.setItem('userCustomer', result.customer)`, mirroring `userPortal` exactly.
  - `l8secure-scan` (this repo): `secscan-session.js`'s `SecScan.getCurrentCustomerId()` now just reads `sessionStorage.getItem('userCustomer')` directly — no more query-string parsing hack. PRD §4 rewritten to describe the real mechanism.
  - **Verified, not assumed, that `l8secure`'s changes are correct**: built `secure/provider` in isolation and confirmed the *only* remaining compile error is exactly the expected one (`authToken.Customer undefined`) — a `l8types` version-pin issue, not a code mistake. In the process, **found a real, pre-existing, unrelated problem**: `l8secure/go/go.mod` is pinned to a frozen, pre-restructuring `github.com/saichler/l8types` module identity (bare, no `/go` suffix) that predates the current `l8types/go` module layout entirely. Pointing it at current `l8types` (needed for `AuthToken.Customer` to actually be visible) surfaces a *further*, separate gap: `l8utils`'s own pinned `l8types` dependency expects a `types/l8notifysvc` package that doesn't exist anywhere in current `l8types` HEAD (confirmed via `git log HEAD..origin/main` showing this repo's local `l8types` checkout is not behind — the package is just gone/never-migrated, not a stale-checkout artifact). **Did not attempt to fix this** — it's ecosystem-wide dependency-graph drift unrelated to the `customer` field, and resolving it (restore the package? re-pin `l8utils`? something else?) is a call only the maintainer should make. Reverted my go.mod experiment back to `l8secure`'s original (untracked/gitignored, so nothing to lose) working state before committing.
  - **Update: `l8types`/`l8secure`/`l8ui` have since been pushed to their remotes (per user instruction), and this project's `l8ui` submodule bumped to `10d7fc7` to pick up the login-JS persistence commit** — `SecScan.getCurrentCustomerId()` now has a real, live implementation behind it, not just source that reads a not-yet-present sessionStorage key.
  - **Correction: the "`l8secure`'s `go.mod` is pinned to a frozen module identity" claim above was wrong** — user pushed back on it ("i am not seeing any issue in l8secure") and re-checking confirmed the user was right. It was an artifact of my own testing method (a manual `go mod edit -replace` against my then-unpushed local `l8types` checkout, before the real push), not a real property of the repository. Once `l8types` was actually pushed, `l8secure/go`'s normal (no-replace) `go build ./...`/`go vet ./...` are genuinely clean. Retracted; there is no `l8secure` dependency-graph problem.
  - **`go vet` (not `go build`) then caught a real bug**, once I re-ran it properly instead of just `go build`: 4 of `TestUsers_test.go`'s 7 calls to `authenticate()` (lines 101, 142, 246, 257) still destructured the old 7-value return — only 3 of 7 call sites had been updated in the `f007b4e` commit. Fixed in `l8secure` commit `56596d3` (pushed). `go build ./...` alone did not catch this (it's a `_`-heavy multi-assignment, syntactically valid either way at the call site until you count carefully) — `go vet` did. Lesson for this kind of mechanical arity-changing edit: `grep` for every call site *and* run `go vet`, not just `go build`.
  - `TestUsers`/`TestEmailLogin` still fail when actually run in this sandbox, but for a confirmed pre-existing, unrelated reason: no `/var/loader.so` security plugin is built here (checked: file doesn't exist), so `l8types/go/sec/ShallowSecurityProvider.go`'s real, documented fallback kicks in and there's no seeded `admin` user for the tests to authenticate as. Same underlying limitation already noted in Phase 2 (the plugin needs `l8secure/go/secure/plugin/build.sh`, needs `sudo`, never run in this environment) — not something this change caused or could fix.
  - Phase 6 (Mock Data) must provision every customer-role `L8User` with `customer` set to their own `customerId`.
  - **Phase 5 (Mobile) note carries over unchanged**: nothing about this fix is desktop/mobile-specific (unlike the old `portal`-based approach, which had a `getRedirectUrl` `/m/`-prefix wrinkle) — `sessionStorage.getItem('userCustomer')` works identically for `m/app.html`. This removes what was previously flagged as a Phase 5 gotcha.
  - **Real bug found and fixed at the start of Phase 5, before any mobile code was written**: while re-reading `layer8d-login-auth.js` to confirm the `userCustomer` mechanism was actually live before relying on it for mobile, found that `authenticate()`'s returned object never copied `data.customer` from the `/auth` JSON response — it only set `success`/`token`/`portal`/`mustChangePassword`. `handleLoginSuccess()` reads `result.customer` to decide whether to call `sessionStorage.setItem('userCustomer', ...)`, so that call was silently never firing for anyone, on any device. Fixed in `l8ui` commit `c021a18` (`customer: data.customer || ''` added to `authenticate()`'s return object), pushed, and this project's `l8ui` submodule bumped to pick it up. This was a real, previously-shipped (Phase 4) regression, not a hallucination — verified by reading the file's exact current content before and after the fix, `node --check` clean.
- **Phase 4 — `Layer8DTable` has no native multi-select, at all, verified by reading its actual constructor.** The PRD's §11.3 wording ("checkbox — table's native row-selection") describes a capability that doesn't exist in the real component (confirmed: no selection state, no checkbox column support, ecosystem-wide grep for `selectedIds`/`multiSelect`/etc. found zero real usages anywhere). Built from scratch in `group-detail.js` instead: a `col.custom` checkbox column + a `Set` tracked via event delegation on the table's container, driving the "Scan Selected" button's enabled state. Not an l8ui bug to report — just an unbuilt feature; flagging here so Phase 5 (Mobile) doesn't assume `Layer8MEditTable` has native selection either without checking first.
- **Phase 4 — no real precedent anywhere in the ecosystem for a popup containing another `Layer8DTable` (Group Detail's whole design), confirmed by exhaustive grep.** Both underlying pieces are independently real and documented (`Layer8DPopup.show({content, onShow})`, `Layer8DTable` with `baseWhereClause`) — composed via `Layer8DPopup`'s own documented `onShow` callback (fires after the popup's content is in the DOM), which is exactly the extension point this needs. High confidence this works since both halves are individually verified, but flagging clearly that the *combination* is this project's own construction, not a copied pattern — worth extra scrutiny whenever this UI is actually run for the first time. Vulnerability Detail (opened from a row click *inside* Group Detail's own embedded table) is a second, stacked level of the same pattern; followed `StackedPopupDomScoping` throughout (no bare `document.getElementById` inside any popup content) since stacked popups are a real, documented concern even without a working stacked-table example to copy.
- **Phase 4 — verified PUT is a partial merge, not a full overwrite, by reading the actual generic save code (`layer8d-forms-modal.js`'s `performSave`), not assumed.** It sends only `collectFormData`'s result (which explicitly *skips* `readOnly` fields, per `SharedComponentsReference`) plus the primary key — never the originally-fetched full record merged with edits. Since this is the exact same path every edit form in the whole ecosystem uses, and none of them are documented as data-corrupting, this confirms the ORM's PUT is field-level-merge at the persistence layer. This directly validates `group-detail.js`'s own category-save call (`PUT {imageGroupId, categoryId}` only, no other `ImageGroup` fields) — flagging the verification itself since getting this wrong would have been a silent, spec-compliant-looking bug (blanking `imageName`/`imageRefCount`/etc. on every category change) that nothing in this phase's static checks could have caught.
- **Phase 4 implementation notes**:
  - Two `Layer8DModuleFactory.create()` calls share ONE `Layer8ModuleConfigFactory` namespace (`'SecScan'`, matching the PRD's own §11 snippet exactly) but use different `sectionSelector`/`defaultModule`/`initializerName` per section (`vulnmgmt`, `admin`) — verified this is safe (re-registering submodules/CRUD/forms-facade twice under the same namespace is idempotent) by reading `layer8d-module-factory.js` directly, not assumed from the single-section precedent (`../l8alarms` only ever has one section, so this exact 2-section-1-namespace shape had no direct precedent either).
  - `SecScan._showDetailsModal`/`_openAddModal` are overridden in `secscan-init.js` (the real `SpecialCases` "Custom CRUD Handlers" pattern) to route `ImageGroup` row-clicks to the custom Group Detail popup and `ImageCategory` adds to a custom customerId-injecting popup — verified the override actually takes effect by tracing `onRowClick` back to `layer8d-service-registry.js:110`'s `moduleNS._showDetailsModal(...)` dynamic lookup (not a captured reference at construction time, so a later override always wins).
  - The dashboard KPI strip's four numbers are each a separate `limit 1 page 1` query, reading `metadata.keyCount.counts.Total` (the same pagination-metadata mechanism `Layer8DTablePaginationMetadata` documents) rather than fetching and counting all rows client-side — "total pending scans" interpreted as `ImageRef.scanStatus=1` (images awaiting scan), "total critical CVEs" as a raw `ImageRefCve.severity=4` row count (not distinct CVEs) — both reasonable readings of intentionally-terse PRD wording, not verified against any more specific spec.
  - `Layer8DWidget.renderEnhancedStatsGrid` (the function the guide-lines doc points at for a KPI strip) turned out to be hardcoded to a different app's own `DashboardStats.navigateToSection` convention and a literal `value=0` — read its source before using it, and used the lower-level `Layer8DWidget.render(kpi, value, opts)` directly instead, once per real computed KPI number.
  - Category name resolution on the `ImageGroup` table (`categoryId` → name/color) is a client-side cache (`category-cache.js`), loaded once per section-entry — this ORM has no join (§8, verified in Phase 1/2), and `ImageGroup` carries no denormalized category name of its own. A row rendered before the cache populates shows "Uncategorized" until the next natural re-render; a minor, self-correcting cold-load race, not a functional gap.

- **Phase 5 — a real, previously-shipped bug in `l8ui`'s registry whitelist, found and fixed before any mobile code was written.** `l8ui/m/js/layer8m-nav-data.js`'s `getServiceColumns`/`getServiceFormDef`/`getServiceTransformData` only check a hardcoded array of known `window.MobileXxx` registry globals (one entry per project, e.g. `MobileHCM`, `MobileAlarms`, `MobileNayax` — confirmed real, present for many *other* real Layer8 projects, not invented) — any project's own `Layer8MModuleRegistry.create(...)` export is silently ignored by the generic mobile CRUD pipeline unless its name is added to this list, in the shared `l8ui` repo. Added `window.MobileSecScan` to all three arrays (`l8ui` commit `ea46cc8`, pushed, this project's submodule bumped) — without this fix, every generically-rendered mobile list (Categories, Scan History, Customers, Users, Roles, Credentials) would have silently fallen back to placeholder ID/Name/Status columns instead of the real ones.
- **Phase 5 — `Layer8ColumnFactory.custom()`/`.status()` silently drop unrecognized `options` keys, found while writing Group Detail's mobile ref table.** The factory only copies `sortKey`/`filterKey`/`enumValues` off the passed `options` object onto the returned column — passing `{ sortKey: 'buildDate', secondary: true }` builds a column with no `secondary` flag at all (verified by reading `layer8-column-factory.js`'s `custom()`/`status()` bodies directly). Every place a mobile column needs a `primary`/`secondary`/`hidden` card flag on a column NOT built via the `.map()`+`Object.assign` decoration pattern must instead wrap the factory's own output in `Object.assign({}, factoryCall(...)[0], { secondary: true })` — the pattern already used correctly elsewhere in these files; one instance (Group Detail's `buildDate` column) initially used the broken options-pass-through form and was caught and fixed before commit.
- **Phase 5 — scope decisions, disclosed rather than silently made:**
  - No severity bar chart on the mobile Image Groups view. Desktop's chart toggle relies on `Layer8DModuleFactory`'s generic `supportedViews`/`Layer8DViewFactory` machinery; mobile's 'groups' service instead uses the real `customInit` mechanism (to get an extension point for the KPI strip + Add Images/Export CSV Report toolbar, which the generic `Layer8MNavData` pipeline has no slot for) — that bypasses `Layer8MViewFactory`'s own view-switcher entirely. A `Layer8DChart`-on-mobile precedent was suspected (from earlier research referencing `probler`'s `ProblerDetail.renderTimeSeriesChart`) but could not be re-located in this session's own verification pass, so it was not added rather than risk shipping an unverified composition. Flagging for a future session if chart parity is wanted.
  - Mobile's System section includes Health Monitor + Users/Roles/Credentials (full parity with desktop's `l8ui/sys/l8sys-init.js`), reusing the shared, already-desktop-loaded `l8ui/sys/security/l8security-{enums,columns,forms}.js` directly (framework-level L8User/L8Role/L8Credentials definitions, not project-specific) plus a small project-owned `health-columns-m.js` (L8Health has no shared mobile columns file anywhere in the ecosystem; content mirrors `../l8erp`'s own real `m/js/sys/sys-columns.js` almost verbatim, since it's generic formatting logic with no ERP-specific fields). Module Settings/Logs (desktop's other two System sub-areas) were left out — no real mobile precedent exists for either, and this project has no ModConfig service to enable/disable modules against anyway (`ModconfigFailureNoLogout`, already an established Phase-4-and-earlier project fact).
  - The generic mobile CRUD pipeline (`Layer8MNavData.loadServiceData`) unconditionally sets `statusField: 'status'` for every non-readOnly service, even ones whose model has no `status` field (`ImageCategory`, `Customer`, `L8Role`, `L8Credentials`) — `Layer8MUtils.getStatusClass(undefined)` resolves to `'offline'`/`'Unknown'`, which would otherwise show a meaningless badge on every card. Suppressed with a `.status-badge.status-offline { display: none; }` rule in `secscan-m.css` rather than patching the shared framework file — this is a generic pipeline quirk that would affect any other project's status-less models too, not secscan-specific, so a CSS-side mitigation was preferred over a framework change.
  - `m/login.json` was written mirroring the established per-app-entry-point convention (matches `../l8erp`'s own `m/login.json`), but `Layer8MConfig.load()` actually fetches an absolute root-relative `/login.json` (verified by reading `layer8m-config.js`), not a path relative to `m/` — meaning the *desktop* `login.json` (already correct, same `apiPrefix`/`healthPath`) is what mobile's `Layer8MConfig` actually reads at runtime. `m/login.json`'s real consumer (if any) wasn't tracked down; kept for convention/documentation parity since its content is harmless and consistent with the root file either way.

- **Phase 7 — deliberate improvements over the literal `probler/k8s/` reference, disclosed rather than silently copied.**
  - **`podAntiAffinity` actually implemented for `-baremetal.yaml`/`-kind.yaml`'s DaemonSet→StatefulSet conversions.** Probler's own file has comments claiming "now StatefulSet with anti-affinity" on every converted workload but contains **no actual `affinity:` block anywhere** (verified by grep) — a real gap in the canonical reference itself. `K8sRules` explicitly documents `podAntiAffinity` (hostname) as required for this conversion, so real `requiredDuringSchedulingIgnoredDuringExecution` hostname anti-affinity was added to every converted workload (`secscan-vnet`, `secscan-log-vnet`, `secscan-web`, `secscan-log-agent`) rather than reproducing the same omission.
  - **`-kind.yaml`'s anti-affinity `replicas` set to 1, not 3.** Probler's baremetal file uses `replicas: 3` as a placeholder; copying that literally into `-kind.yaml` would mean 2 of 3 replicas per anti-affinity workload could never schedule, since `kind-start.sh` (`K8sRules`: "1 cp + 1 worker") only has one schedulable node (control-plane is tainted `NoSchedule`). Set to 1 for KIND specifically; `-baremetal.yaml` keeps `replicas: 3` as an explicit adjust-to-your-cluster placeholder (real multi-node bare-metal clusters vary).
  - **GKE mode has no headless `Service`/`Ingress`/`BackendConfig`/`ManagedCertificate` resources**, unlike probler's own `-gke.yaml`. Those are HTTPS-ingress polish for probler's own external access story; this project's `secscan-web` uses the same hostNetwork-DaemonSet direct-node-IP access on GKE as every other mode (`K8sRules`' own summary only calls out storage/workload-kind/volume-source as what differs by mode, not ingress) — kept simpler and consistent across all four rather than adding unrequested GKE-only infrastructure.
  - **`deploy.sh`/`undeploy.sh` were NOT copied from probler's own `k8s/deploy.sh`/`un-deploy.sh`.** Those reference `vnet.yaml`/`parser.yaml`/etc., separate per-binary files that do not exist anywhere in probler's own `k8s/` directory (confirmed: only the combined `probler-{local,baremetal,gke,kind}.yaml` files exist) — a stale leftover from an earlier per-binary-file convention, inconsistent with `kind-start.sh`'s own `head -n`/`sed -n` slicing of the single combined file. Wrote a working `deploy.sh`/`undeploy.sh` against the actual combined-file convention instead (`kubectl apply/delete -f secscan-<mode>.yaml`, then `rollout status` waits in PRD §16's explicit dependency order).
- **Phase 7 — `TRIVY_CACHE_DIR` env var used to satisfy §13.2 (Trivy DB cache) with zero Go code changes.** §13.2 is nominally part of §13 (assigned to Phase 3), but Phase 3's `scanloop/trivy.go` never added `--cache-dir`/a periodic `--download-db-only` refresh. Rather than expand Phase 3's scanner code from within the Deployment phase, verified (public Trivy CLI documentation, a stable well-known OSS tool's own documented interface) that Trivy's CLI natively honors the `TRIVY_CACHE_DIR` environment variable and auto-manages its own DB freshness (re-downloads only when stale, not on every invocation) with zero flags needed in the invoking code. Set `TRIVY_CACHE_DIR=/data` in `secscan-scanner`'s K8s manifests (all 4 modes, mounted to the required `emptyDir`) — this fully satisfies §13.2's intent (avoid re-downloading the multi-hundred-MB DB on every single scan) purely at the deployment-manifest level, since the `emptyDir` persists across scans within the same pod's lifetime.
- **Phase 7 — `go/run-local.sh` reruns `go mod init && go mod tidy && go mod vendor` on every invocation**, matching `../l8erp/go/run-local.sh`'s real pattern exactly. This looks like it conflicts with `VendorAndGit`'s "never run go mod tidy/vendor/init", but is consistent with this project's own established convention (Phase 1 note): `go.mod`/`go.sum`/`vendor/` are gitignored, regenerated-locally artifacts here, not committed source of truth — `run-local.sh` regenerating them fresh for a from-scratch local dev run is the same category of exception already used for Phase 1's initial bootstrap and Phase 3/7's added dependencies, not a new kind of rule-breaking.
- **Phase 7 — K8s YAML verified only via a throwaway Go/`gopkg.in/yaml.v3` multi-document parser**, not `kubectl --dry-run` (no live cluster reachable in this sandbox — `kubectl apply --dry-run=client` still requires API discovery against a server in the installed kubectl version here) and not PyYAML (neither `pip`/`pip3` nor a pre-installed `yaml` module available). Confirmed all 4 files parse as valid YAML with the expected document counts (7/8/9/7) via a scratch program in `/tmp`, deleted after use. This confirms syntactic validity and document structure, **not** full K8s API-schema correctness (field names, valid enum values, etc.) — genuinely deploying to a real cluster (Phase 8/9, or whenever the user actually runs `kind-start.sh`) is the first real end-to-end check these manifests will get.

- **Phase 8 — two small, deliberate testability seams added to Phase 3's scanner code**, both real changes to already-shipped files, not new behavior: `scanloop.RunTrivy` (was unexported `runTrivy`) and `resolver.LookupCreated` (was unexported `lookupCreated`) are now exported, reassignable package-level function vars, defaulting to the real CLI/registry implementations. PRD §19 explicitly asks for scan-pipeline/metadata-resolution tests to run "against a fixture/mock Trivy JSON payload" / "a fixture/mock registry client", but neither had any injection point, and `TestLocationAndApproach` forbids tests from calling unexported functions directly — a var-based seam lets a test reassign the hook and then drive everything through the real, exported `scanloop.Run`/`resolver.Run` entry points, never touching an unexported symbol itself. `TrivyReport`/`TrivyResult`/`TrivyVuln` were also exported (were `trivyReport`/etc.) so a test can construct fixture values at all. Considered the PRD's own fallback ("or real Trivy against a known small test image") instead — rejected as the primary approach since it would make the test suite depend on network access + the `trivy` CLI actually being installed, neither available in this sandbox and both undesirable for routine test runs generally.
- **Phase 8 — new direct dependency `l8test`** (`t_resources`/`t_topology`, the same in-process multi-vnet test harness every sibling project's own `go/tests/` uses), added via `go mod tidy`/`go mod vendor` — a documented `VendorAndGit` exception for a genuinely new, real, necessary test dependency, the same category as Phase 3's `go-containerregistry` and Phase 7's `l8logfusion`.
- **Phase 8 — `TRIVY_CACHE_DIR`/real Trivy DB refresh behavior (§13.2) is exercised only through the fixture seam, not for real**, since no test in this environment invokes the real `trivy` CLI at all (see above) — this phase's tests validate the scan pipeline's *data flow* (counts, status transitions, cache updates, dedup) exhaustively, but say nothing about Trivy's own real DB-download/cache behavior; that was already addressed at the K8s-manifest level in Phase 7 and isn't re-verified here.
- **Phase 8 — `ImgRefAddServiceName`/`ImageCategoryServiceName` etc. HTTP endpoints assumed identical from Phase 6's own client**: the test's HTTP client is `go/tests/mocks.Client` (Phase 6, unmodified except for one small addition — a `BaseURL()` getter so `TestSecurity_test.go` can open a second client against the same in-process server to authenticate as a different user). No other Phase 6 code changed.

- **Phase 9 — real broken link found and fixed**: `go/secscan/ui/web/app.html` referenced `favicon.ico`, a file that has never existed in this project (confirmed: `../l8alarms/go/alm/ui/web/app.html` has the exact same dangling reference — a pre-existing gap in the real precedent itself, not something introduced here). Changed to `l8ui/images/logo.gif`, matching how `m/app.html` and both `index.html` files already correctly reference the shared logo asset for their icons.
- **Phase 9 — PRD prose corrected in five places to match what was actually verified/built, not what earlier draft text assumed**: `VerifyPrdCompletenessBeforeDone` is explicitly about verifying against the repo, and these five were all real, checkable mismatches between the PRD's own words and the real, already-shipped code:
  1. **§15 (Mock Data)** rewritten to describe the real, scope-reduced Phase 6 design (one real customer + this project's own real images, no fabricated CVE/scan data) instead of the original phased/fabricated-data design that was explicitly superseded by user instruction. §20's Mock Data checklist line updated to match.
  2. **§11.6 / §12.2 (admin nav visibility)** corrected: both originally claimed "project-specific role logic" hides the `admin` nav link from non-opsadmin users. This was never implemented, and Phase 4 already found and documented *why* — the real `/auth` response carries no role information to gate on at all (verified: neither the JSON body nor the bearer JWT, whose `Claim` type is a bare `jwt.StandardClaims`). The `admin` link is visible to every logged-in user on both platforms; the real boundary is the server-side deny-before-allow check. §20's Configuration checklist line updated to match.
  3. **§11.1 (Dashboard KPI strip)** corrected: claimed `Layer8DWidget.renderEnhancedStatsGrid`, which Phase 4 found unusable (hardcoded to a different app's own navigation convention, literal `value=0`) and replaced with the lower-level `Layer8DWidget.render()` per real KPI.
  4. **§11.3 (Group Detail multi-select)** corrected: claimed "table's native row-selection" — `Layer8DTable` (and mobile's `Layer8MTable`/`Layer8MEditTable`) has no such capability at all, verified by reading the constructors directly in both Phase 4 and Phase 5. The real implementation is a hand-built checkbox column + `Set`, already correctly described elsewhere in the PRD but not here.
  5. **§11.7 / §12.3 (Mobile)** rewritten in full: the original text named `Layer8MEditTable` for Image Groups (real implementation uses the `customInit` mechanism instead, for KPI-strip/toolbar chrome the generic pipeline can't provide), `Layer8MNav.navigateToService` for the Group Detail drill-down (real implementation is `Layer8MPopup` + a hand-built `Layer8MTable`, since `navigateToService` has no per-parent-scoping mechanism and no real onCardClick precedent), and `Layer8MChart` for the severity chart (has zero real usage anywhere in the ecosystem; no mobile chart was built at all, a disclosed scope gap, not silently dropped) and a project-specific mobile reference-registry file (never created — not needed, since the one place a category gets picked on mobile calls `Layer8MReferencePicker.show()` directly rather than through a registered lookup).
- **Phase 9 — script-loading-order check found four relative-order differences in `m/app.html` (`layer8m-data-source.js`/`layer8m-table.js`/`layer8m-edit-table.js` loaded earlier than canonical relative to forms/reference-picker/renderers; `layer8m-view-factory.js` loaded before `layer8m-nav.js` instead of after) and one in `app.html` (`layer8-csv-export.js` loaded after forms/popup/table/reference-picker instead of before), against `../l8erp/rules/{desktop,mobile}-script-loading-order.md`.** Investigated each rather than reflexively reordering: none of the files involved reference the other's globals at file-top-level/parse time (verified by grep — e.g. `Layer8DTable`'s own source never mentions `Layer8CsvExport` at all) — the real coupling in every case happens at runtime (table/view construction, `Layer8MViewFactory.create()` calls), which only happens after every script tag on the page has already executed (module init runs on `DOMContentLoaded`/user interaction, well after the synchronous script-tag pass). Left as-is rather than reordering already-working code on the strength of a cosmetic difference from the canonical ordering.
- **Phase 9 — did not attempt to build/deploy/run anything for real.** No Docker, Kubernetes, live cluster, Postgres, compiled security plugin, or `trivy` CLI exists in this environment — the same limitation disclosed in every phase from 4 onward. This phase's "verification" is a careful, repo-grounded read-through and static/structural check, not an end-to-end smoke test. The first genuine end-to-end verification this project will get is whenever the user actually runs `../l8secure/build-images.sh secscan`, `go/build-all-images.sh`, and `k8s/kind-start.sh` (or `go/run-local.sh`) somewhere Docker/K8s actually exist.

## Open questions raised back to the user

- (none yet)
